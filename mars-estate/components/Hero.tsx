import React from "react";

type Props = {};

const Hero = (props: Props) => {
  return (
    <div className="w-full h-screen bg-hero-background bg-cover bg-center"></div>
  );
};

export default Hero;
